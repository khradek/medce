
<div class="medium-container">
  <table class=" table">
    <tr>
      <th>User</th>
      <th></th>
    </tr>

    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= link_to "Login As", user_masquerade_path(user), class: "btn btn-gray btn-xs" %></td>
        </td>
      </tr>
    <% end %>

  </table>
</div>